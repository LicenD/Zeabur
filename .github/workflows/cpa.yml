name: CPA
on:
  schedule:
    - cron: "10 23 * * *" 
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_ENV"

      - name: Get latest release tag + asset URLs
        run: |
          set -euo pipefail
          API="https://api.github.com/repos/router-for-me/CLIProxyAPI/releases/latest"
          JSON="$(curl -fsSL "$API")"

          TAG="$(echo "$JSON" | jq -r '.tag_name')"
          if [ -z "${TAG:-}" ] || [ "$TAG" = "null" ]; then
            echo "Failed to get tag_name from latest release"
            exit 1
          fi
          echo "TAG=$TAG" >> "$GITHUB_ENV"
          echo "Latest TAG=$TAG"

          URL_AMD64="$(echo "$JSON" | jq -r '.assets[] | select(.name | test("linux_amd64\\.tar\\.gz$")) | .browser_download_url' | head -n1)"
          URL_ARM64="$(echo "$JSON" | jq -r '.assets[] | select(.name | test("linux_arm64\\.tar\\.gz$")) | .browser_download_url' | head -n1)"

          if [ -z "${URL_AMD64:-}" ] || [ "$URL_AMD64" = "null" ]; then
            echo "Cannot find linux_amd64.tar.gz in latest release assets"
            exit 1
          fi
          if [ -z "${URL_ARM64:-}" ] || [ "$URL_ARM64" = "null" ]; then
            echo "Cannot find linux_arm64.tar.gz in latest release assets"
            exit 1
          fi

          echo "URL_AMD64=$URL_AMD64" >> "$GITHUB_ENV"
          echo "URL_ARM64=$URL_ARM64" >> "$GITHUB_ENV"
          echo "AMD64 asset: $URL_AMD64"
          echo "ARM64 asset: $URL_ARM64"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Skip if this release tag already exists in GHCR
        run: |
          set -e
          IMAGE="ghcr.io/${OWNER_LC}/cpa:${TAG}"
          echo "Checking: $IMAGE"
          if docker manifest inspect "$IMAGE" > /dev/null 2>&1; then
            echo "Already built for ${TAG}, skip."
            echo "SKIP=true" >> "$GITHUB_ENV"
          else
            echo "Not built for ${TAG}, will build."
            echo "SKIP=false" >> "$GITHUB_ENV"
          fi

      - name: Set up Docker Buildx
        if: env.SKIP != 'true'
        uses: docker/setup-buildx-action@v3

      - name: Generate Dockerfile (Debian + bash, rename program+dir to CPA)
        if: env.SKIP != 'true'
        run: |
          set -euo pipefail
          mkdir -p ctx

          cat > ctx/Dockerfile <<'EOF'
          FROM debian:bookworm-slim

          ARG URL_AMD64
          ARG URL_ARM64
          ARG TARGETARCH

          RUN apt-get update && apt-get install -y --no-install-recommends \
                ca-certificates tzdata curl tar bash findutils \
              && rm -rf /var/lib/apt/lists/*

          # Use bash for RUN (supports pipefail)
          SHELL ["/bin/bash", "-lc"]

          ENV TZ=Asia/Shanghai
          RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone

          # Download correct arch tarball and install binary as /CPA/CPA
          RUN set -euxo pipefail; \
              mkdir -p /tmp/app /CPA; \
              case "$TARGETARCH" in \
                amd64) url="$URL_AMD64" ;; \
                arm64) url="$URL_ARM64" ;; \
                *) echo "Unsupported TARGETARCH=$TARGETARCH" && exit 1 ;; \
              esac; \
              echo "Downloading for $TARGETARCH: $url"; \
              curl -fsSL "$url" -o /tmp/app/app.tar.gz; \
              echo "Tar list (head):"; \
              tar -tzf /tmp/app/app.tar.gz | head -n 80; \
              tar -xzf /tmp/app/app.tar.gz -C /tmp/app; \
              echo "Extracted tree (maxdepth 3):"; \
              find /tmp/app -maxdepth 3 -type f -printf "%M %p\n" | head -n 200; \
              \
              bin="$(find /tmp/app -maxdepth 5 -type f -perm -111 \( -name 'CLIProxyAPI' -o -name 'CLIProxyAPI*' \) | head -n 1)"; \
              if [[ -z "$bin" ]]; then \
                echo "CLIProxyAPI* not found, fallback to any executable file"; \
                bin="$(find /tmp/app -maxdepth 5 -type f -perm -111 | head -n 1)"; \
              fi; \
              [[ -n "$bin" ]] || (echo "Executable binary not found in release tarball" && exit 1); \
              echo "Using binary: $bin"; \
              cp "$bin" /CPA/CPA; \
              chmod +x /CPA/CPA; \
              chown -R 1000:1000 /CPA; \
              rm -rf /tmp/app

          WORKDIR /CPA
          EXPOSE 8317

          USER 1000:1000
          CMD ["./CPA"]
          EOF

      - name: Build & push multi-arch (tag + latest)
        if: env.SKIP != 'true'
        run: |
          set -euo pipefail
          IMAGE="ghcr.io/${OWNER_LC}/cpa"

          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            --build-arg URL_AMD64="${URL_AMD64}" \
            --build-arg URL_ARM64="${URL_ARM64}" \
            -t "${IMAGE}:${TAG}" \
            -t "${IMAGE}:latest" \
            --push \
            ctx
